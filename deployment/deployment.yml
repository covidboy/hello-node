apiVersion: apps/v1
kind: Deployment
metadata:
  name: node
  namespace: node
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node
  template:
    metadata:
      labels:
        app: node
    spec:
      containers:
      - name: nodejs
        image: ssubhabrata/myubuntu:node-hello-world-v1
        # command:
        # - sh
        # - "-c"
        # - echo Hello Kubernetes! && sleep 3600